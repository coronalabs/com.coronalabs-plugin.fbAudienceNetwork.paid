Sources for the plugin `plugin.fbAudienceNetwork.paid`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.fbAudienceNetwork.paid" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
